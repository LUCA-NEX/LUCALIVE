# Copy to ".env" here (backend/) OR put ".env" in the repository root (parent of backend/).
# Both are loaded: root first, then backend/.env overrides. Never commit real cookies.

# Douyu: paste the full Cookie header value from your browser after logging in to douyu.com
# (same as DouyinLiveRecorder config 「斗鱼cookie」). Keep on ONE line; quotes optional.
LUCALIVE_DOUYU_COOKIES=
